{
  "name": "cypress-xpheno",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo 'Starting server' && npm-run-all --parallel start-server",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test:chrome": "npx cypress run --browser chrome --headed",
    "test:firefox": "npx cypress run --browser firefox",
    "test:edge": "npx cypress run --browser edge",
    "build": "npm run cy:run",
    "ci:chrome": "npm run build -- --browser chrome --record",
    "ci:firefox": "npm run build -- --browser firefox --record",
    "ci:edge": "npm run build -- --browser edge --record"

    
  
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.2",
    "cypress-mochawesome-reporter": "^3.7.0",
    "cypress-xpath": "^2.0.1",
    "faker": "^5.5.3",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "cypress-parallel": "^0.14.0"
  }
}
